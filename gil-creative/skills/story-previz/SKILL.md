---
name: story-previz
description: |
  시네마틱 프리비즈 스킬 — 카메라 무빙·렌즈·조명을 지정한 숏 리스트를 설계한다. 달리·팬·틸트·크레인·핸드헬드 같은 카메라 무빙 어휘로 촬영 전 프리비즈 릴을 구성한다. 콘티(정지 프레임) 다음 단계이며, 생성 실행·크레딧은 gil-creative에 위임한다. 트리거: "프리비즈", "시네마틱 숏", "프리비즈 릴"
version: "2.2.1"
origin: moai-cowork@f1eb954
---

# story-previz: 시네마틱 프리비즈

## 스킬 개요(상세)

시네마틱 프리비즈 스킬 — 카메라 무빙·렌즈·조명을 지정한 숏 리스트를 설계한다. 달리·팬·틸트·크레인·핸드헬드 같은 카메라 무빙 어휘로 촬영 전 프리비즈 릴을 구성한다. 콘티(정지 프레임) 다음 단계이며, 생성 실행·크레딧은 gil-creative에 위임한다.
다음과 같은 요청 시 반드시 이 스킬을 사용하세요:
- "프리비즈", "시네마틱 숏", "프리비즈 릴"
- "카메라 무빙", "달리", "팬", "틸트", "크레인 샷"
- "렌즈 선택", "숏 리스트"


콘티(정지 프레임) 이후 단계에서 **카메라 연출을 구체화**하는 스킬이다. 렌즈·조명·무빙을 지정해 촬영 전 프리비즈 릴(숏 리스트)을 만든다. `story-conti`와 병합하지 않는 이유는 산출물 매체가 다르기 때문이다 — conti는 정지 보드, previz는 카메라가 움직이는 클립이다. **생성 실행·크레딧은 하지 않는다** — `gil-creative`에 위임한다.

## 1. 개요

프리비즈는 **무엇을 어떤 카메라로 잡을지(렌즈·구도)**와 **어떤 무빙을 넣을지(달리·팬·틸트·트래킹)**를 결정한다. 숏 리스트로 정리해 촬영 계획의 기준을 만든다.

## 2. 이 스킬이 하지 않는 것

- **영상·클립 생성·크레딧 고지·모델 선택 안 함** — `gil-creative:higgsfield-core` 계약에 위임(§4-생성 실행).
- **정지 프레임 콘티 안 만듦** — 화각별 정지 보드는 `story-conti`.
- **대본·씬 확정 안 함** — 씬은 `story-screenplay`가 확정.
- **모델 프롬프트 문법 안 소유** — 벤더별 문법은 `gil-creative`.

## 3. 사전 확인

- `references/camera-move-grammar.md` — 카메라 무빙 어휘·렌즈·숏 리스트 표 규격.
- `${CLAUDE_PLUGIN_ROOT}/skills/story-webtoon-art/references/context-budget.md` — 여러 숏을 한 세션에서 다룰 때 컨텍스트 체크포인트 인계 원칙(참조).

## 4. 워크플로우

### Step 1 — 숏 스펙 정의

씬별로 렌즈(광각·표준·망원)·조명·카메라 무빙을 지정한다. 각 숏이 무엇을 강조하는지(공간·인물·감정) 한 줄로 명시.

### Step 2 — 카메라 무빙 설계

무빙 어휘(달리·팬·틸트·크레인·핸드헬드 등 — `camera-move-grammar.md`)로 각 숏의 움직임을 지정한다. 무빙은 감정·정보에 복무해야 한다("멋있어서" 넣지 않는다).

### Step 3 — 숏 리스트 정리

씬 → 숏 단위로 렌즈·무빙·길이·목적을 표로 정리한다. 이것이 프리비즈 릴의 설계도다.

### Step 4-생성 실행 — gil-creative 위임

숏 클립의 실제 생성·비용 프리플라이트·모델 선택은 `gil-creative:higgsfield-core` 계약(`models_explore` 라이브 조회 + `get_cost` 사전 고지)에 위임한다.

> gil-creative 미설치 시: 완성 숏 프롬프트를 텍스트로 출력하고 Higgsfield 웹(https://higgsfield.ai)에서 직접 생성하도록 안내한다.

### Step 5 — 프리비즈 릴 조합

숏·연출 메모를 씬별 프리비즈 릴로 정리한다.

## 5. 출력 형식

```markdown
## 숏 리스트 — [작품] [씬 범위]

| 숏 | 렌즈 | 카메라 무빙 | 화각 | 길이 | 목적 |
|----|------|-------------|------|------|------|
| 1 | 광각 24mm | 슬로 달리 인 | WS→MS | ~4s | 공간→인물 진입 |
| 2 | 망원 85mm | 고정 | CU | ~2s | 감정 정점 |

### 숏별 생성 프롬프트
[숏 1 복붙 프롬프트]
```
→ 생성 실행: `gil-creative:higgsfield-core` 위임.

## 6. 주의사항

- **생성 전 비용 고지·승인.** `gil-creative`의 `get_cost` 결과를 사용자에게 고지하고 승인받은 뒤 생성한다.
- **렌즈·조명 톤 통일** — 한 씬 안에서 왜곡·색온도가 흔들리지 않게.
- **무빙은 목적에 복무** — 무의미한 카메라 이동 금지.
- **클립은 최소 필수 숏만** — 생성 비용이 크므로 핵심 숏 위주.

## 7. 관련 스킬

### Before
- `story-conti` — 정지 프레임 콘티
- `story-screenplay` — 씬 확정

### After
- `story-ip-pitch` — 영상화 피칭(부가)

### 위임
- `gil-creative:higgsfield-core` — 생성 실행·비용 프리플라이트·모델 선택

## 8. References

| 파일 | 로드 조건 |
|------|-----------|
| `references/camera-move-grammar.md` | 카메라 무빙·렌즈·숏 리스트 규격이 필요할 때 |
| `../story-webtoon-art/references/context-budget.md` | 멀티 숏 세션 컨텍스트 절약(참조) |

## 9. 출처

- 카메라 무빙·렌즈 어휘: 영상 촬영 실무 관행(references에 정리).
- 컨텍스트 체크포인트 인계: aitoon-comic(내부 자산 참조).
- 생성·비용·모델: `gil-creative:higgsfield-core` 라이브 계약.
- 원작 카피라이터: **조남경** (https://www.facebook.com/Bmisty)
